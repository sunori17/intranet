from django.apps import AppConfig


class ConsolidacionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.consolidacion'
    verbose_name = 'Consolidación de Notas'